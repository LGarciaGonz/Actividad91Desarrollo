import { createContext } from "react";

const CartContext = createContext(null);
const ItemContext = createContext(null);
const EventContext = createContext(null);

export {CartContext, ItemContext, EventContext};